# Backend for API + Socket.IO.
# Your socket + API requests go HERE. If this is Render, push + redeploy the `server/` on Render after changing game logic — otherwise you still get old behavior (e.g. lobby 1/6).
# For full local stack: run `server` on :5000 and set:
# VITE_SERVER_URL=http://localhost:5000
VITE_SERVER_URL=https://uno-no-mercy-wq92.onrender.com
VITE_APP_NAME=UNO No Mercy